function LoadingPage() {
  return (
    <div>
      <h1>Loading...</h1>
    </div>
  );
}

export default LoadingPage;

// como la pagina de not-found.jsx, loading.jsx es una pagina que va a cargar por defecto hasta que consigamos el resultado que querramos, en este caso, llamada a una Api en post/page.js funcion: async function loadPosts()
